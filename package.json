{
  "name": "fovea",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "build:node": "tsc",
    "test": "npm run build:node && node dist/cpu/reference.test.js && node dist/engine/tokenizer.test.js"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "@webgpu/types": "^0.1.40",
    "typescript": "^5.5.0",
    "vite": "^6.0.0"
  }
}
